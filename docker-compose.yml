services:
  app:
    image: ghcr.io/pdelfino0/gremio-brasileiro-2025:latest
    ports:
      - "8080:80"
    restart: unless-stopped
    # Para desenvolvimento local, descomente o build:
    # build:
    #   context: .
    #   dockerfile: Dockerfile 